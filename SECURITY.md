# Security Playbook

This repository includes security scanning and guidance.

Quick actions:
- Rotate secrets (ADMIN_TOKEN, SECRET_KEY) and store in external secrets manager.
- Run `tools/security_scan.sh` locally to produce artifacts in `artifacts/security`.
- Review CI artifacts uploaded by `Security CI` workflow.

Contact: repo maintainers (use Issues in repo)
