[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "leftrightbarrelroll"
version = "0.1.0"
description = "Generate unicode homoglyph and invisible variants for testing and research"
authors = [ { name = "", email = "" } ]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
leftrightbarrelroll = "leftrightbarrelroll:main"
